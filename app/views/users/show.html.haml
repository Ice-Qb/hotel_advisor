- provide(:title, @user.name)
.row
  %aside.span4
    %section
      %h1
        = @user.name
  .span8
    - if @user.hotels.any?
      %h3
        Hotels (#{@user.hotels.count})
      %ol.hotels
        = render @hotels
      = will_paginate @hotels